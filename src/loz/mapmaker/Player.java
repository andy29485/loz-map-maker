package loz.mapmaker;

import java.util.List;

/**
 * Class that represents the player of the game
 * 
 * @author az
 *
 */
public class Player extends GameCharacter {
  /**
   * List of item IDs that the player is carrying.
   *
   * May not be the best idea... but then again this is just the map editor.
   */
  private List<Integer> inventory;
  // TODO create player(at some point)
}
